import { Box, Typography } from '@mui/material';

const ResourceExplorer = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Resource Explorer</Typography>
      <Typography>Browse and manage AWS resources</Typography>
    </Box>
  );
};

export default ResourceExplorer;
